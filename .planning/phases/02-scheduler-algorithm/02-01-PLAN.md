---
phase: 02-scheduler-algorithm
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_scheduler.py
autonomous: true
requirements:
  - DEL-01
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05

must_haves:
  truths:
    - "tests/test_scheduler.py exists and is importable without Streamlit"
    - "All 9 test functions are defined and FAIL (RED) when run against current models.py — confirming the contract"
    - "No test file imports streamlit, app, or any non-stdlib module besides models"
  artifacts:
    - path: "tests/test_scheduler.py"
      provides: "Full pytest test suite for scheduler behavior"
      exports:
        - test_generate_schedule_returns_schedule
        - test_high_priority_scheduled_before_low
        - test_task_exceeding_budget_goes_to_skipped
        - test_frequency_2_produces_two_blocks
        - test_timeblock_has_start_end_reason
        - test_timeblock_end_after_start
        - test_explain_returns_string
        - test_explain_contains_task_title
        - test_explain_empty_schedule
  key_links:
    - from: "tests/test_scheduler.py"
      to: "models.py"
      via: "import models"
      pattern: "^import models$"
---

<objective>
Write the complete pytest test suite for the Phase 2 scheduler algorithm — before any implementation exists. These tests define the behavioral contract that Plan 02 must satisfy.

Purpose: TDD red phase — tests fail now, drive the implementation in Plan 02.
Output: tests/test_scheduler.py with 9 failing tests covering SCHED-01 through SCHED-05 and DEL-01.
</objective>

<execution_context>
@/Users/aidangarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aidangarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scheduler-algorithm/02-RESEARCH.md
@.planning/phases/02-scheduler-algorithm/02-VALIDATION.md

<interfaces>
<!-- Existing contracts in models.py that tests must build against -->

From models.py (project root):
```python
PRIORITY_ORDER: dict[str, int] = {"high": 0, "medium": 1, "low": 2}

@dataclass
class Pet:
    name: str
    species: str        # "dog" | "cat"
    age: int
    preferences: dict = field(default_factory=dict)

@dataclass
class Owner:
    name: str
    available_hours: int
    pets: list = field(default_factory=list)

@dataclass
class Task:
    title: str
    duration_minutes: int
    priority: str       # "high" | "medium" | "low"
    frequency: int = 1
    pet: "Pet | None" = None
```

New classes that tests reference (not yet in models.py — Plan 02 will add them):
```python
# models.Scheduler(owner, tasks).generate_schedule() -> models.Schedule
# models.Schedule.blocks: list[TimeBlock]
# models.Schedule.skipped: list[Task]
# models.Schedule.explain() -> str
# models.TimeBlock.start_time: datetime.time
# models.TimeBlock.end_time: datetime.time
# models.TimeBlock.reason: str
# models.TimeBlock.task: Task
```
</interfaces>
</context>

<feature>
  <name>Scheduler test suite</name>
  <files>tests/test_scheduler.py</files>
  <behavior>
    - test_generate_schedule_returns_schedule: Scheduler(owner, tasks).generate_schedule() returns a Schedule instance
    - test_high_priority_scheduled_before_low: Given [low_task, high_task], high_task block appears before low_task block in schedule.blocks
    - test_task_exceeding_budget_goes_to_skipped: Owner with 0 available_hours — task goes to schedule.skipped, schedule.blocks is empty
    - test_frequency_2_produces_two_blocks: Task with frequency=2 produces exactly 2 TimeBlocks in schedule.blocks
    - test_timeblock_has_start_end_reason: TimeBlock has start_time, end_time (both datetime.time), reason (non-empty str)
    - test_timeblock_end_after_start: block.end_time > block.start_time for a 20-minute task starting at 08:00
    - test_explain_returns_string: schedule.explain() returns a non-empty str when blocks exist
    - test_explain_contains_task_title: schedule.explain() contains the task's title in the output string
    - test_explain_empty_schedule: schedule.explain() returns a str (does not raise) when schedule.blocks is empty
  </behavior>
  <implementation>
    Write tests/test_scheduler.py now. Use only `import models` and `import datetime` — no Streamlit, no other imports.

    Helper functions (module-level, not classes):
    - `make_owner(hours=2)` — returns Owner(name="Alex", available_hours=hours)
    - `make_pet()` — returns Pet(name="Mochi", species="dog", age=3)

    All 9 test functions are provided verbatim in the RESEARCH.md "Test structure for test_scheduler.py" section. Copy them exactly — do not rewrite. The exact names match the VALIDATION.md task map.

    After writing the file, run the RED-phase check:
    `.venv/bin/python -m pytest tests/test_scheduler.py -x 2>&1 | head -20`

    Expected: Tests are collected but FAIL with AttributeError (models.Scheduler does not exist yet). If pytest itself errors (import error of a different kind), fix the import. Do NOT write any implementation in models.py yet — that is Plan 02's job.

    Commit with message: `test(02-01): add failing scheduler tests (RED)`
  </implementation>
</feature>

<verification>
- tests/test_scheduler.py exists and is syntactically valid
- `import models` in the test file uses no Streamlit dependency
- `.venv/bin/python -m pytest tests/test_scheduler.py --collect-only` collects exactly 9 tests
- `.venv/bin/python -m pytest tests/test_scheduler.py -x` FAILS (RED) — models.Scheduler not yet defined
- `.venv/bin/python -m pytest tests/test_models.py -v` still passes all 8 existing tests (models.py not modified)
</verification>

<success_criteria>
9 tests collected, all failing with AttributeError on models.Scheduler. The 8 existing model tests remain green. No Streamlit import in test file.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scheduler-algorithm/02-01-SUMMARY.md`
</output>
